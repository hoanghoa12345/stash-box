services:
  functions:
    build: ./supabase
    container_name: stashbox-functions
    image: stashbox/functions:latest
    volumes:
      - ./supabase/functions:/app/functions
    working_dir: /app/functions/metatags-info
    env_file:
      - ./supabase/.env.local
    ports:
      - 8080:8080